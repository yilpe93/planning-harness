# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

AI 기반 제품 기획 워크플로우 자동화 시스템. 문제 정의 하나만 입력하면 페르소나 → Journey Map → 아이디어 → 지표/Aha 가설 → 사용자 선택 → PRD까지 순서대로 자동 실행된다.

## 참고 문서

| 문서 | 경로 | 내용 |
| ---- | ---- | ---- |
| 사용 설명서 (사용자용) | `docs/USAGE.md` | 단계별 독립 실행 방법, 트리거 키워드, 출력 예시, FAQ |
| 오케스트레이터 스킬 | `.claude/skills/planning-harness/SKILL.md` | 전체 실행 순서·병렬 dispatch·재생성 플로우 상세 규칙 |
| 단계 스킬 | `.claude/skills/planning-*/SKILL.md` | 각 Step별 입력·출력·제약 상세 |
| 병렬 워커 에이전트 | `.claude/agents/planning-idea-brief.md` | Step 5·6 병렬 처리 규칙 |

## 언어 및 커뮤니케이션 규칙

- **기본 응답 언어**: 한국어
- **문서화**: 한국어로 작성
- **변수명/함수명**: 영어 (코드 표준 준수)

## 워크플로우 구조 (README.md 기준 9단계)

```
Step 1  문제 정의
Step 2  고객 페르소나 3개
Step 3  Journey Map (페르소나별)
Step 4  HMW 6개 + 아이디어 12개 (법적 문제되는 것 제외, 기능적 외부 요인 검토)
        ↓ 각 아이디어마다 Step 5·6을 병렬 작업
Step 5  AARRR 가설 (아이디어별, 컴팩트)
Step 6  Aha Moment 가설 (아이디어별, 컴팩트)
Step 7  [결과물 아이디어는 마크다운 표로] 사용자 아이디어 선택 ← 유일한 수동 개입 포인트
Step 8  1-Pager PRD (선택된 아이디어)
Step 9  PRD 초안 (선택된 아이디어)
```

**핵심**: Step 5·6은 12개 아이디어 전체에 대해 가설 수준으로 병렬 제공 → 사용자가 지표·Aha 가설을 보고 Step 7에서 선택 → Step 8·9는 선택된 아이디어에만 집중.

### 각 단계 핵심 규칙

| 단계               | 규칙                                                                |
| ------------------ | ------------------------------------------------------------------- |
| Step 1 문제 정의   | 형식 `[누구]가 [상황]에서 [무엇]을 하려 하지만 [무엇] 때문에 [불편]을 겪는다.` / 없으면 1회만 질문 |
| Step 3 Journey Map | 감정 1~2점 구간 `⚠️ MVP` 표시 — 이 행만 Step 4 HMW의 근거           |
| Step 4 아이디어    | 제약: 예산 1천만 원 이하 / 3주 MVP / AI 핵심 기능 필수              |
| Step 5 AARRR       | 불확실하면 유사 서비스 사례로 방향·범위 제시                        |
| Step 6 Aha Moment  | `[사용자]가 [시간] 안에 [행동]을 경험하면 [감탄]를 느낀다` 형식     |
| Step 7 선택        | `AskUserQuestion` 도구로 선택(복수 가능) — 유일한 수동 개입, 선택 전까지 Step 8 대기 |
| Step 9 PRD 초안    | 성공 기준 숫자 필수 / Out of Scope 3개 이상 / 불확실도 ★~★★★ 섹션별 |

### Journey Map 라벨 상세

| 라벨 | 의미 | 기준 |
| ---- | ---- | ---- |
| `[MoT]` | Moments of Truth — 결정적 순간 | 처음 10초 / 첫 성공(Aha) / 실패·오류 / 결제 직전 / 이탈 직전 — 5가지 중 해당 단계 |
| `[TOP]` | 가장 크고 반복적인 Pain | 페르소나당 1개만 표시 |
| `⚠️ MVP` | 감정 점수 1~2점 구간 | Step 4 HMW의 유일한 근거 — 반드시 표시 |

## 스킬 시스템

`.claude/skills/` 아래 로컬 스킬로 기획 프로세스를 자동화한다. 단계 스킬은 `Skill` 도구로 호출하고, 병렬 워커는 `Agent` 도구로 dispatch한다.

| 스킬                   | 역할           | 트리거 상황                                                |
| ---------------------- | -------------- | ---------------------------------------------------------- |
| `planning-harness`     | 오케스트레이터 | "처음부터 끝까지", "전체 기획 실행", "/planning-harness"   |
| `planning-persona`     | Step 2         | "페르소나 만들어줘", "타깃 유저 정의" 등 독립 실행         |
| `planning-journey-map` | Step 3         | "Journey Map", "여정 지도", "Pain Point 분석" 등 독립 실행 |
| `planning-hmw`         | Step 4         | "아이디어 뽑아줘", "HMW 만들어줘" 등 독립 실행             |
| `planning-aarrr`       | Step 5         | "AARRR", "North Star Metric", "성장 지표" 등 독립 실행     |
| `planning-aha`         | Step 6         | "Aha Moment", "온보딩", "첫 성공 경험" 등 독립 실행        |
| `planning-prd`         | Step 8·9       | "PRD", "기획서", "1-Pager", "제품 요구사항" 등 독립 실행   |

### 에이전트 시스템 (병렬 처리)

`.claude/agents/` 아래 병렬 워커로 Step 5·6을 동시 처리한다. `Agent` 도구로 dispatch한다.

| 에이전트              | 역할                                            | 호출 방식                                                                                          |
| --------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `planning-idea-brief` | Step 5·6 병렬 워커 (아이디어별 AARRR + Aha 가설) | 오케스트레이터가 12개 아이디어를 3개씩 4배치로 나눠 4개 동시 dispatch → 결과를 `04-idea-brief.md`로 취합 |

## 출력 파일 구조

```
planning-output/
└── YYYYMMDD-[slug]/              # slug: 핵심 키워드 2~3개, 소문자-하이픈
    ├── 01-personas.md
    ├── 02-journey-maps.md
    ├── 03-hmw-ideas.md
    ├── 03-hmw-ideas-v2.md        # Step 7 미선택 → 1차 재생성
    ├── 04-idea-brief.md          # 12개 아이디어별 AARRR + Aha 가설 병렬 브리핑
    ├── 04-idea-brief-v2.md       # 1차 재생성
    ├── 05-prd.md                 # 선택된 아이디어의 1-Pager + PRD 초안
    └── [slug]-기획결과-전체.md   # 전체 합본
```

> 같은 날 같은 slug로 재실행하면 동일 디렉터리의 기존 파일을 덮어쓴다(특정 단계만 재실행 시 해당 파일만 갱신).

### 파일 네이밍 특수 케이스

| 케이스 | 동작 | 파일명 예시 |
| ------ | ---- | ----------- |
| 단일 아이디어 선택 | 기본 파일명 그대로 | `05-prd.md` |
| 복수 아이디어 동시 선택 | 아이디어별 별도 파일 (`-아이디어A`, `-아이디어B` 접미사) | `05-prd-아이디어A.md` |
| Step 7 미선택 재생성 | 원본 보존, 버전 suffix 추가 | `03-hmw-ideas-v2.md` |

> 복수 선택 예: "3번과 7번 모두 진행해줘" → 각각 별도 파일 생성, 합본에는 두 PRD 순서대로 병합

## 훅 (자동 동작)

`.claude/settings.local.json`에 설정된 훅이 자동 수행한다.

| 훅                   | 동작                                                                                                       |
| -------------------- | ---------------------------------------------------------------------------------------------------------- |
| `PostToolUse(Write)` | `planning-output` 경로 저장 시 `[기획 하네스] ✓ 저장: 파일명` 출력                                          |
| `Stop`               | 세션 종료 시 저장된 `.md` 목록 출력 + PRD(`05-prd.md`)까지 끝났는데 합본(`*-기획결과-전체.md`)이 없으면 ⚠️ 경고 |

## 역할 설정 관례

| 단계                   | 역할                          |
| ---------------------- | ----------------------------- |
| 페르소나 · Journey Map | "서비스 디자이너 10년차"      |
| HMW · 아이디어         | "창업 5회 경험의 제품 기획자" |
| AARRR                  | "스타트업 그로스 해커 8년차"  |
| Aha Moment             | "제품 분석가 7년차"           |
| PRD                    | "SaaS PM 10년차"              |
